{"instinct_candidates":[{"id":"scope-roundtrip-contract","title":"Treat scope parse/format as a strict roundtrip contract","trigger":"When adding or modifying Loom memory scope parsing/formatting (especially new scope kinds or new accepted spellings)","action":"Define an explicit internal representation for scopes and require `parse -> format -> parse` roundtrips to be stable; reject/raise on invalid inputs rather than guessing; keep one canonical string form for each scope to avoid drift in stored memory metadata.","confidence":0.62,"tags":["memory","scopes","parsing","ux","correctness"]},{"id":"scope-kind-explicit-prefixes","title":"Prefer explicit scope kind prefixes over heuristics","trigger":"When accepting user-provided scope strings (CLI flags, config, or interactive prompts)","action":"Require explicit prefixes like `file:...` / `command:...` / `dir:...` (or whatever set Loom supports) instead of trying to infer intent from the value; document the accepted prefixes and keep them minimal to reduce ambiguity and surprise.","confidence":0.58,"tags":["memory","cli","ux","parsing"]},{"id":"scope-normalize-paths-cautiously","title":"Normalize path scopes cautiously and deterministically","trigger":"When implementing path-like scopes (file/dir/repo-ish) or changing how they are serialized","action":"Normalize only what is safe and deterministic (e.g., strip whitespace, unify separators if needed, avoid environment-dependent resolution); avoid implicit `resolve()` / cwd-dependent behavior unless the scope kind explicitly means 'relative to cwd'.","confidence":0.5,"tags":["memory","scopes","determinism","portability"]},{"id":"scope-validation-errors-high-signal","title":"Make scope validation errors actionable","trigger":"When adding new validation rules to scopes","action":"Raise errors that include: the original scope string, the expected grammar/prefix, and 1-2 example valid forms; ensure error text is stable enough for CLI UX tests.","confidence":0.55,"tags":["memory","cli","errors","testing"]}],"skill_candidates":[{"name":"loom-memory-scope-extension","description":"Procedure for adding or changing Loom memory scope kinds without breaking stored data or CLI UX.","body":"# Loom Memory Scope Extension\n\nGoal: add/adjust a memory scope kind with stable parsing, canonical formatting, and low-noise errors.\n\n## Steps\n\n1. Inspect the scope model and parser\n- Read `src/agent_loom/memory/scopes.py`.\n- Identify: (a) the canonical set of scope kinds, (b) parsing entrypoint(s), (c) formatting/serialization function(s).\n\n2. Add/modify a scope kind\n- Implement one canonical string form (what gets stored and displayed).\n- Avoid heuristics: require an explicit prefix for the scope kind.\n- Keep behavior deterministic (no implicit cwd/FS resolution unless the kind semantics demand it).\n\n3. Enforce strict validation\n- On invalid scope strings: raise/return an error that includes the input, the expected prefix/grammar, and at least one valid example.\n- Do not silently coerce ambiguous inputs.\n\n4. Roundtrip invariants\n- Ensure `parse(scope_str)` + `format(scope_obj)` is stable.\n- Ensure `parse(format(scope_obj))` yields an equivalent object.\n\n5. Update call sites\n- If CLI accepts scopes, ensure help text and examples match the canonical string form.\n- If scopes are persisted, ensure existing persisted values still parse (or provide an explicit migration path).\n\n6. Gates\n- Run: `uv run ruff check .`\n- Run: `uv run basedpyright`\n- Run: `uv run pytest`\n\n## Done when\n- One canonical string form exists per scope kind.\n- Invalid inputs produce high-signal, testable errors.\n- Roundtrip invariants hold and are covered by tests.\n","tags":["loom","memory","scopes","parsing","testing"],"source_instinct_ids":["scope-roundtrip-contract","scope-kind-explicit-prefixes","scope-validation-errors-high-signal","scope-normalize-paths-cautiously"]}]}
