---
id: packet:ralph-ticket-critph28-20260503T084309Z
kind: packet
packet_kind: ralph
status: consumed
target: ticket:critph28
mode: execution
change_class: protocol-authority
risk_class: medium
style: reference-first
verification_posture: observation-first
iteration: 1
created_at: 2026-05-03T08:43:09Z
updated_at: 2026-05-03T08:45:34Z
scope:
  kind: repository
  repositories:
    - repo:root
child_write_scope:
  records:
    - None - child returns output only; parent reconciles ticket, evidence, critique, and packet status.
  paths:
    - skills/loom-critique/templates/critique-packet.md
parent_merge_scope:
  records:
    - ticket:critph28
  paths:
    - .loom/tickets/20260503-critph28-quote-critique-packet-placeholders.md
    - .loom/evidence/20260503-critique-packet-placeholder-validation.md
    - .loom/critique/critique-packet-placeholder-safety-review.md
    - .loom/packets/ralph/20260503T084309Z-ticket-critph28-iter-01.md
source_fingerprint:
  git_commit: 4be9a107580927850b9d6c589b3da985b262f5a4
  integration_remote: origin
  integration_ref: origin/main
  integration_commit: 4be9a107580927850b9d6c589b3da985b262f5a4
  git_status_summary: dirty_mixed
  git_status_detail: parent-owned ticket and packet records are modified/untracked for launch; child write-scope file is clean relative to 4be9a10
  compiled_from:
    - ticket:critph28
    - ticket:shipacc1
    - ticket:gitstat26
    - plan:skills-corpus-context-integrity-hardening-pass
    - initiative:skills-corpus-context-integrity-hardening-pass
    - research:skills-corpus-third-pass-follow-up-validation
execution_context:
  branch: main
  push_remote: origin
  worktree: /Users/alexanderbutler/code_projects/personal/agent-loom
  isolation: none
  git_shared_metadata_mutations: forbidden
  destructive_commands: forbidden
  network: forbidden
context_budget:
  posture: tight
  max_source_files: 3
  max_excerpt_lines_per_file: 180
  avoid_full_file_reads: true
sources:
  initiative:
    - initiative:skills-corpus-context-integrity-hardening-pass
  research:
    - research:skills-corpus-third-pass-follow-up-validation
  plan:
    - plan:skills-corpus-context-integrity-hardening-pass
  ticket:
    - ticket:critph28
  files:
    - skills/loom-critique/templates/critique-packet.md
    - skills/loom-records/references/packet-frontmatter.md
links: {}
---

# Mission

Make critique packet frontmatter placeholders consistently safe and quoted.

# Bound Context

`ticket:critph28` covers
`initiative:skills-corpus-context-integrity-hardening-pass#OBJ-029`. Critique
packets are copied during review work, so placeholder values in copyable
frontmatter should be YAML-safe until filled and should use explicit `<TBD: ...>`
replacement instructions where placeholder scalars matter.

# Source Snapshot

The critique packet template already uses quoted `<TBD: ...>` placeholders for
many fields, but several copyable frontmatter fields still use unquoted generic
placeholders such as `<UTC timestamp>`, `<sha or unknown>`, `<record ref>`, and
`<name|unknown>`. `review_target.summary` is also an unquoted plain placeholder.

The template body already points to `skills/loom-records/references/packet-frontmatter.md`
and warns against fake precision for unavailable Git or execution details.

# Change Class

Protocol authority, medium risk. The edit changes high-copy template guidance and
must not change critique packet semantics or add any validator/runtime.

# Verification Targets

- `initiative:skills-corpus-context-integrity-hardening-pass#OBJ-029`
- `ticket:critph28#ACC-001`
- `ticket:critph28#ACC-002`
- `ticket:critph28#ACC-003`
- `ticket:critph28#ACC-004`

# Task For This Iteration

Make the smallest edit in `skills/loom-critique/templates/critique-packet.md`
that:

1. Quotes remaining copyable frontmatter placeholder scalar values where YAML
   safety matters.
2. Prefer explicit `<TBD: ...>` placeholder wording for replacement instructions.
3. Keeps the template aligned with current critique packet grammar.
4. Does not add fake precision or turn placeholder choices into runtime
   requirements.
5. Does not change critique packet semantics or add a validator.

# Verification Posture

Observation-first. Before and after editing, report targeted observations for:

- unquoted frontmatter placeholders;
- quoted `<TBD: ...>` placeholders;
- references to current packet-frontmatter grammar;
- warnings against fake precision / runtime requirements.

Run `git diff --check` after editing.

# Stop Conditions

Stop and return `blocked` or `escalate` if:

- satisfying the ticket would require changing critique packet semantics;
- the edit would add a validator, runtime, schema, or required command surface;
- replacing placeholders would introduce fake precision instead of safe
  replacement instructions;
- satisfying the ticket would require changes outside the critique packet
  template.

Do not run `git fetch`, remote edits, Git config edits, destructive commands, or
shared Git metadata mutations. Do not edit parent-owned `.loom` records.

# Output Contract

Return outcome, files changed, records changed, before/after observations,
`git diff --check` result, self-review, blockers/residual risks, and recommended
ticket state/next route.

# Working Notes

Parent compiled from clean `main` at `4be9a10` after closing and pushing
`ticket:retmem27`. Saving this packet and activating the ticket made the worktree
`dirty_mixed` before child launch; those parent-owned `.loom` records are outside
child write scope. `skills/loom-critique/templates/critique-packet.md` remains
clean relative to `4be9a10` at launch.

# Child Output

Outcome: `stop`

Files changed:

- `skills/loom-critique/templates/critique-packet.md`

Records changed by child:

- None. Parent retained ticket, evidence, critique, and packet reconciliation.

Before observations:

- Unquoted frontmatter placeholders: 16 matches, including `summary`, timestamps,
  `source_fingerprint`, `compiled_from`, and `execution_context` fields.
- Quoted `<TBD: ...>` placeholders: 12 matches.
- Packet-frontmatter grammar reference was present.
- Fake-precision/runtime warnings were present, including `unknown` / `none`
  guidance and the warning not to invent branch, remote, worktree, or
  command-policy precision.

After observations:

- Unquoted frontmatter placeholders: 0 matches.
- Quoted `<TBD: ...>` placeholders: 29 matches; `id` now uses a quoted scalar
  with embedded replacement instructions.
- Packet-frontmatter grammar reference remains present.
- Fake-precision/runtime guidance is unchanged; no validator, runtime, schema, or
  command requirement was added.

Verification:

- `git diff --check` passed with no output.

Self-review:

- Stayed within `child_write_scope.paths`.
- Did not modify `.loom` records.
- Did not run forbidden Git/network/destructive operations.
- Did not change critique packet semantics or add validator/runtime/schema.

Blockers or residual risks:

- No blockers.
- Parent still needs to reconcile ticket, evidence, and mandatory critique
  disposition.

Recommendation:

- Move `ticket:critph28` to `review_required` for mandatory critique.

# Parent Merge Notes

- 2026-05-03T08:45:34Z: Parent accepted the bounded implementation output,
  recorded `evidence:critique-packet-placeholder-validation`, marked this packet
  `consumed`, and moved `ticket:critph28` to `review_required` for mandatory
  critique.
