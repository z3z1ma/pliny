---
"id": "al-58c0"
"status": "review"
"deps":
- "al-18ec"
- "al-8d66"
- "al-f968"
"links": []
"created": "2026-02-15T19:15:03Z"
"type": "task"
"priority": 1
"assignee": "z3z1ma"
"parent": "al-0463"
"tags":
- "sprint:Public-Launch-Architecture-Cleanup"
- "architecture"
- "cli"
- "workspace"
- "team"
"external": {}
---
# Modularize team/workspace CLI command groups

## Objective alignment
`team/cli.py` and `workspace/cli.py` are oversized command entrypoints. Public launch needs thin, readable dispatch layers so command behavior is easier to maintain and extend safely.

## Scope
- Refactor:
  - `src/agent_loom/team/cli.py`
  - `src/agent_loom/workspace/cli.py`
- Extract command handlers into grouped modules (e.g., by noun/domain) while keeping parser UX and command names stable.
- Keep top-level CLI files as parser construction + dispatch orchestration only.

## Non-goals
- No command/flag removals or additions unless required for parity-preserving bug fix.
- No behavior changes to command semantics.
- No refactor of non-CLI business logic beyond required import rewiring.

## Implementation plan
1. Define grouping plan for each CLI:
   - Team examples: lifecycle, inbox, objective/sprint, merge.
   - Workspace examples: repo/worktree, harness, cleanup/snapshot.
2. Move command handler functions into grouped modules and wire imports.
3. Retain one canonical parser builder per CLI with unchanged command tree/help text.
4. Ensure JSON mode and text mode output contracts remain identical to pre-refactor behavior.
5. Remove dead command wrappers and unused imports after migration.

## Verification
- `uv run ruff check .`
- `uv run basedpyright`
- `uv run pytest tests/test_team_cli_ux.py tests/test_workspace_cli_ux.py tests/test_workspace_init.py tests/test_workspace_worktree_parity.py`
- `uv run pytest`

## Acceptance criteria
- `team/cli.py` and `workspace/cli.py` are reduced to thin entrypoint responsibilities.
- Command handlers are organized by domain in dedicated modules.
- CLI help trees and command behavior remain stable in tests.
- JSON/text output behavior remains stable.

## Risks / edge cases
- **Risk:** argparse dispatch wiring errors after handler moves.
  - **Detection:** command-not-found or wrong-handler failures in CLI UX tests.
  - **Mitigation:** migrate command groups incrementally and run targeted tests after each group.
- **Risk:** accidental divergence between parser definitions and handler signatures.
  - **Detection:** runtime argument errors and failing UX tests.
  - **Mitigation:** keep parser+handler changes in the same commit slices; avoid deferred wiring.

## Notes

**2026-02-15T20:23:00Z**

Execution boundary from al-8d66: run only after al-f968 + al-18ec. This ticket owns parser/handler modularization for src/agent_loom/team/cli.py and src/agent_loom/workspace/cli.py. Must consume shared output helpers from al-18ec and must not perform additional team/core domain decomposition beyond required callsite rewires.

**2026-02-15T20:53:43Z**

Analyzed current CLI structure. Team CLI has lifecycle/workers/objective/inbox/merge/util groups. Workspace CLI has harness/repo/worktree/components/deps/impact groups. Next: Create modular command group files.

**2026-02-15T20:54:57Z**

Starting modularization. Created lifecycle.py as proof-of-concept. Will parallelize extraction of remaining command groups using Task tool.

**2026-02-15T21:00:48Z**

Continuing after subagent failure on WorkspaceCore. Existing command-group modules are present; next step is manual validation + wiring into team/workspace cli entrypoints, then checks/tests.

**2026-02-15T21:08:29Z**

Implemented parser/handler modularization and wired both entrypoints to command-group modules.

Created modules:
- team/commands/{lifecycle,workers,objective,inbox,merge,utils}.py
- workspace/commands/{core,repo,harness,worktree,deps,lease}.py

Updated:
- team/cli.py now normalizes argv + builds parser + dispatches imported handlers.
- workspace/cli.py now keeps output/render/parser logic and dispatches imported handlers.

Verification:
- uv run ruff check src/agent_loom/team/cli.py src/agent_loom/team/commands src/agent_loom/workspace/cli.py src/agent_loom/workspace/commands ✅
- uv run basedpyright src/agent_loom/team/cli.py src/agent_loom/team/commands src/agent_loom/workspace/cli.py src/agent_loom/workspace/commands ✅
- uv run --with pytest pytest tests/test_team_cli_ux.py tests/test_workspace_cli_ux.py tests/test_workspace_init.py tests/test_workspace_worktree_parity.py ✅ (49 passed)

Repo-wide checks currently fail due pre-existing unrelated issues:
- uv run ruff check . fails in tests/test_team_harness_omp.py duplicate test definitions
- uv run basedpyright fails on same duplicates + unresolved pytest import in tests/test_server_api_contract.py
- uv run --with pytest pytest (with TEAM_ROLE/TEAM_WORKER_ID unset) fails 5 unrelated tests in compound/init suites

**2026-02-15T21:08:59Z**

Completion candidate.

Commit:
- c877ac5 refactor(cli): modularize team and workspace command handlers

What changed:
- Extracted team handlers into domain modules: lifecycle/workers/objective/inbox/merge/utils.
- Extracted workspace handlers into domain modules: core/repo/harness/worktree/deps/lease.
- Rewired team/cli.py and workspace/cli.py to act as thin parser+dispatch entrypoints with imported handlers.
- Preserved parser tree/command names and json/text dispatch paths.
- Preserved team CLI test monkeypatch contract for inbox_ack via module-level export + runtime module lookup in cmd_inbox_ack.

Commands run:
1) uv run ruff check src/agent_loom/team/cli.py src/agent_loom/team/commands src/agent_loom/workspace/cli.py src/agent_loom/workspace/commands  # pass
2) uv run basedpyright src/agent_loom/team/cli.py src/agent_loom/team/commands src/agent_loom/workspace/cli.py src/agent_loom/workspace/commands  # pass
3) uv run --with pytest pytest tests/test_team_cli_ux.py tests/test_workspace_cli_ux.py tests/test_workspace_init.py tests/test_workspace_worktree_parity.py  # pass (49 passed)
4) uv run ruff check .  # fails (pre-existing unrelated: duplicate tests in tests/test_team_harness_omp.py)
5) uv run basedpyright  # fails (pre-existing unrelated: same duplicate defs + pytest import in tests/test_server_api_contract.py)
6) env -u TEAM_ROLE -u TEAM_WORKER_ID uv run --with pytest pytest  # fails 5 unrelated tests in compound/init suites

Risks / follow-ups:
- Workspace command modules currently delegate output rendering via workspace.cli emit_result/_render_text indirection (runtime import). Functionally stable but could be flattened into shared output helper in a follow-up.
- Repo-wide gate failures are unrelated to this refactor and remain outside ticket scope.
