---
"id": "al-54d3"
"status": "in_progress"
"deps": []
"links": []
"created": "2026-02-16T23:23:19Z"
"type": "task"
"priority": 1
"assignee": "z3z1ma"
"tags":
- "architecture"
- "correctness"
- "refactor"
"external": {}
---
# Cross-module hardening: typed errors, guardrails, and critical path robustness

## Notes

**2026-02-16T23:35:06Z**

Implemented a correctness-first hardening slice spanning core/ticket/memory/workspace/team/dashboard.

Completed:
- Added shared typed error contract in src/agent_loom/core/errors.py (LoomError + LoomWarning + coerce helper).
- Migrated memory/team/workspace module errors to subclass LoomError with mapped http/exit semantics.
- Refactored core git helpers to explicit GitCommandResult; fixed regression paths for missing cwd and quiet failure handling.
- Hardened dashboard API error handling with JSON body validation, typed error coercion, sanitized 5xx responses, and error_id logging.
- Replaced memory edit visibility move silent failure with explicit MemoryError (when visibility explicitly requested) or warning accumulation.
- Tightened ticket parent-chain validation to surface missing-parent invariants as typed TicketArgError in both core and API layers.
- Added workspace worktree remove warning channel: best-effort metadata/lease cleanup failures now return warnings in WorktreeGroupRemoveResult and render in text output.
- Hardened sidecar lifecycle warning visibility in team/core.py tui paths (stop signal/terminate/kill, signal registration, nudge failures, autocapture/objective/done-check/opencode runtime warnings) via sidecar.warning events.
- Expanded architecture guardrails to current hotspots and added max-function-branch complexity thresholds for active hotspot modules.

Validation gates all green after changes:
- UV_CACHE_DIR=.uv-cache uv run --no-sync ruff check .
- UV_CACHE_DIR=.uv-cache uv run --no-sync basedpyright
- UV_CACHE_DIR=.uv-cache uv run --no-sync pytest (324 passed)

Remaining from long-range recomposition plan (tracked for follow-up slices):
- Decompose oversized parser builders (memory/cli.py, workspace/cli_harness.py, team/cli.py, ticket/cli.py, compound/cli.py, pack/cli.py) into command-group specs/registries.
- Split dashboard create_app into blueprints.
- Refactor workspace render_text giant type-switch into renderer registry.
- Continue team sidecar extraction into dedicated runtime package boundaries.
- Compound hook payload normalization strategy interfaces and run_instincts_update phase pipeline decomposition.

**2026-02-16T23:43:05Z**

Second implementation slice complete (behavior-stable decomposition):

1) workspace/render refactor
- Replaced giant isinstance chain with explicit per-result renderer functions + typed registry dispatch in src/agent_loom/workspace/render.py.
- Kept output shape/ordering semantics for all existing render branches.
- Preserved JSON fallback path and prime/context behavior.
- Retained new workspace warning output for worktree remove.

2) compound CLI dispatch refactor
- Replaced monolithic if/elif command chain in src/agent_loom/compound/cli.py with command handler registry (COMMAND_HANDLERS).
- Added shared helpers for error/output handling (_run_catching, _emit_error, _emit_pretty_payload).
- Preserved command-specific UX/output behavior (init/sync human text, hooks semantics, JSON modes, exit codes).

3) Guardrail alignment
- Updated workspace/render size threshold to reflect registry-based decomposition shape while keeping complexity controls.

Measured hotspot effect
- src/agent_loom/workspace/render.py: max function branch complexity now 17 (was ~145 in render_text chain).
- src/agent_loom/compound/cli.py: max function branch complexity now 14 (was ~90 in main chain).

Quality gates
- UV_CACHE_DIR=.uv-cache uv run --no-sync ruff check .
- UV_CACHE_DIR=.uv-cache uv run --no-sync basedpyright
- UV_CACHE_DIR=.uv-cache uv run --no-sync pytest
All green (324 passed).

**2026-02-16T23:44:51Z**

Follow-up decomposition/hardening slice complete:

- workspace/render.py: converted to per-result renderer helpers + registry dispatch (TEXT_RENDERERS), removing giant type-switch concentration.
- compound/cli.py: replaced monolithic main if/elif chain with command registry (COMMAND_HANDLERS) and shared error/output wrappers.
- team/core.py sidecar (tui): converted additional silent exception paths in bounce/inbox/stall/orphan loops to explicit sidecar.warning events and log entries.
- Guardrails: tightened complexity ceilings for refactored hotspots (workspace/render.py and compound/cli.py) to lock in reduced complexity.

Current measured hotspot complexity:
- workspace/render.py max function branch complexity: 17
- compound/cli.py max function branch complexity: 14

Validation:
- UV_CACHE_DIR=.uv-cache uv run --no-sync ruff check .
- UV_CACHE_DIR=.uv-cache uv run --no-sync basedpyright
- UV_CACHE_DIR=.uv-cache uv run --no-sync pytest
All green (324 passed).

**2026-02-16T23:57:20Z**

Third decomposition slice complete:

Dashboard architecture recomposition:
- Split src/agent_loom/dashboard/app.py route registration into explicit blueprints:
  - health/capabilities/introspect
  - tickets
  - team
  - memory
  - workspace
  - compound
- Kept existing route paths and envelope shape stable.
- Preserved typed/sanitized API error behavior via shared _api_error closure and centralized error handler registration.

Memory CLI decomposition + correctness hardening:
- Refactored src/agent_loom/memory/cli.py command execution from large if/elif chain to registry dispatch (_RUN_HANDLERS).
- Added typed _EditRunOptions dataclass and _build_edit_options(), replacing mutable argparse.Namespace patching in edit/append flows.
- Kept CLI semantics/exit codes unchanged; memory UX tests remain green.

Guardrail updates:
- Tightened dashboard complexity threshold to reflect blueprint split.
- Tightened memory/cli complexity threshold after dispatch decomposition.
- Adjusted size ceilings to current post-refactor file sizes while preserving anti-regression checks.

Validation:
- UV_CACHE_DIR=.uv-cache uv run --no-sync ruff check .
- UV_CACHE_DIR=.uv-cache uv run --no-sync basedpyright
- UV_CACHE_DIR=.uv-cache uv run --no-sync pytest
All green (324 passed).

**2026-02-17T00:07:46Z**

Implemented next architecture slice for workspace harness parser hardening:
- Deduplicated all repo-selection option blocks via _add_selection_flags(...).
- Extracted command-group parser builders for repo/set/lease/worktree/snapshot/deps/impact so add_harness_parser now composes helpers instead of owning every subcommand block.
- Preserved command names/flags and command handlers; behavior stability verified by full gates.

Verification:
- UV_CACHE_DIR=.uv-cache uv run --no-sync ruff check .
- UV_CACHE_DIR=.uv-cache uv run --no-sync basedpyright
- UV_CACHE_DIR=.uv-cache uv run --no-sync pytest (324 passed)

**2026-02-17T00:17:03Z**

Implemented ticket-core correctness decomposition focused on update workflow:
- Refactored ticket.core.update() into staged helpers with explicit boundaries: conflict validation, parent-chain validation, dependency graph validation/update, link replace/incremental symmetric link mutation, body/title mutation, and persistence.
- Preserved existing CLI behavior and error messages for parent/dependency/link invariants while reducing monolithic complexity.
- Added shared helper functions (_validate_update_conflicts, _validate_parent_chain, _apply_dependency_update, _apply_incremental_link_update, etc.) to keep future edits localized and testable.

Validation:
- UV_CACHE_DIR=.uv-cache uv run --no-sync ruff check .
- UV_CACHE_DIR=.uv-cache uv run --no-sync basedpyright
- UV_CACHE_DIR=.uv-cache uv run --no-sync pytest (324 passed)

**2026-02-17T00:18:34Z**

Additional ticket CLI architecture refinement:
- Moved ticket command dispatch map to a module-level `_HANDLERS` registry in ticket/cli.py and simplified `_dispatch()` to use the shared registry.
- This keeps routing deterministic and avoids rebuilding the handler map per invocation while preserving command behavior and errors.

Validation rerun after this change:
- UV_CACHE_DIR=.uv-cache uv run --no-sync ruff check .
- UV_CACHE_DIR=.uv-cache uv run --no-sync basedpyright
- UV_CACHE_DIR=.uv-cache uv run --no-sync pytest (324 passed)

**2026-02-17T00:23:14Z**

Workspace harness parser architecture refinement:
- Further decomposed add_harness_parser into dedicated command-group builders: init, exec, sandbox, cleanup, repo lifecycle, components, services, and deepen.
- Introduced a shared worktree add/ensure parser builder to remove duplicated option declarations while preserving command UX.
- Added shared refresh-index parser helper to deduplicate components/services alias wiring.
- No command/flag surface changes; behavior remains stable.

Validation:
- UV_CACHE_DIR=.uv-cache uv run --no-sync ruff check .
- UV_CACHE_DIR=.uv-cache uv run --no-sync basedpyright
- UV_CACHE_DIR=.uv-cache uv run --no-sync pytest (324 passed)

**2026-02-17T00:25:00Z**

Core git helper coherence pass:
- Removed unused `run_quiet` dead code from core/git.py.
- Added `git_quiet_result(...)` as an explicit quiet-result layer and made `git_quiet(...)` delegate to it.
- Centralized checked-command error conversion via `_stdout_or_raise(...)` to deduplicate `git_checked` and `git_checked_env` logic.
- Behavior remains stable while reducing hidden coupling and duplicated failure formatting paths.

Validation:
- UV_CACHE_DIR=.uv-cache uv run --no-sync ruff check .
- UV_CACHE_DIR=.uv-cache uv run --no-sync basedpyright
- UV_CACHE_DIR=.uv-cache uv run --no-sync pytest (324 passed)

**2026-02-17T00:29:00Z**

Dashboard correctness hardening + hotspot decomposition:
- Routed team/memory/workspace/compound blueprint exception paths through shared `api_error` mapping instead of ad-hoc `400` + raw error string responses.
- Improved default HTTP status mapping in `_api_error`: `FileNotFoundError` -> 404, `PermissionError` -> 403, typed/read errors remain explicit.
- Added `WorkspaceReadError` to known typed error classification for stable client-facing workspace failures.
- Extracted static capability endpoint catalog to `dashboard/capabilities.py` and consumed it from `dashboard/app.py`, reducing hotspot file size while preserving response shape.
- Preserved route paths and envelope contracts (`ok(data=..., meta=...)` / `err(...)`).

Validation:
- UV_CACHE_DIR=.uv-cache uv run --no-sync ruff check .
- UV_CACHE_DIR=.uv-cache uv run --no-sync basedpyright
- UV_CACHE_DIR=.uv-cache uv run --no-sync pytest (324 passed)

**2026-02-17T00:38:05Z**

Ticket workflow modularization:
- Extracted update mutation pipeline from ticket/core.py into new module: `ticket/update_workflow.py`.
- Introduced typed `TicketUpdateSpec` contract and `apply_ticket_update_workflow(...)` orchestration.
- Moved invariant/validation/mutation/persistence stages into dedicated helpers in the workflow module (parent chain, dependency cycle validation, symmetric link updates, body/title updates, save transaction).
- Simplified `ticket.core.update(...)` to store + spec wiring + workflow invocation.
- Preserved CLI/API behavior and error semantics while reducing hotspot coupling and improving stage clarity.

Validation:
- UV_CACHE_DIR=.uv-cache uv run --no-sync ruff check .
- UV_CACHE_DIR=.uv-cache uv run --no-sync basedpyright
- UV_CACHE_DIR=.uv-cache uv run --no-sync pytest (324 passed)

**2026-02-17T00:44:28Z**

Team lifecycle warning hardening:
- Added `_record_runtime_warning(...)` helper in team/core.py to emit structured `run.warning` events for non-fatal operational failures.
- Replaced selected silent `except ...: pass` paths in critical orchestration flows with explicit warning events:
  - manager bootstrap merge-target hint derivation
  - doctor integrator-dirty check path
  - capture pane-id refresh persistence
  - ship manager-notification delivery
- Preserved command behavior (best-effort semantics) while making partial failures observable in run events.

Validation:
- UV_CACHE_DIR=.uv-cache uv run --no-sync ruff check .
- UV_CACHE_DIR=.uv-cache uv run --no-sync basedpyright
- UV_CACHE_DIR=.uv-cache uv run --no-sync pytest (324 passed)

**2026-02-17T00:48:48Z**

Hardened team spawn-integrator force-recovery path in src/agent_loom/team/core.py by replacing five silent except-pass blocks with structured run.warning events (kill window, remove path worktree, remove branch worktree, delete branch, prune metadata). Behavior remains best-effort and non-fatal, but failures are now observable for diagnostics. Full gates remain green: ruff, basedpyright, pytest (324 passed).

**2026-02-17T00:52:40Z**

Refactored memory visibility-move handling in src/agent_loom/memory/core.py into explicit helper functions (_old_base_for_note_rel, _move_note_for_visibility, warning formatter). Implicit reconciliation failures now emit deterministic warning text with id/from/to/error context; explicit --visibility moves still fail with MemoryError(CONFLICT) and richer details. Added regression tests in tests/test_memory_notes_merge.py for implicit warning and explicit conflict error semantics. Full gates green (ruff, basedpyright, pytest 326 passed).

**2026-02-17T00:58:32Z**

Hardened dashboard API write boundaries in src/agent_loom/dashboard/app.py: strict JSON body validation, typed integer parsing for priority fields, and sanitized error mapping so non-Loom exceptions no longer leak raw messages (unknown failures return internal error + error_id). Added new module src/agent_loom/dashboard/requests.py to reduce app hotspot size and keep request parsing logic centralized. Added server tests for invalid priority, invalid JSON, and sanitized internal failures in tests/test_server_auth.py. Guardrail restored (dashboard/app.py back under threshold) and full gates are green: ruff, basedpyright, pytest (329 passed).

**2026-02-17T01:02:55Z**

Core git helper hardening: introduced GitStdoutResult + git_stdout_result in src/agent_loom/core/git.py and replaced active quiet-string call paths with explicit command-result handling. git_scoped_commit now resolves HEAD via checked path (no silent empty fallback). Updated ticket default_assignee to use git_stdout_result instead of git_quiet. Added regression coverage in tests/test_core_helpers.py for failure-path detail preservation. Full gates green: ruff, basedpyright, pytest (330 passed).

**2026-02-17T01:05:28Z**

Refactored compound hook adapter normalization in src/agent_loom/compound/hooks.py by extracting shared primitives for non-empty string selection, mapping selection, tool field extraction (tool_name/tool_input/tool_response), and outcome/reason mapping. Opencode and OMP normalizers now use a shared strategy pipeline while preserving adapter-specific session/event/prompt semantics. Added tests in tests/test_compound_hooks_normalization.py for opencode fallback extraction and OMP is_error override precedence. Full gates green: ruff, basedpyright, pytest (332 passed).

**2026-02-17T01:07:38Z**

Decomposed workspace harness parser surface by extracting full parser construction implementation to src/agent_loom/workspace/cli_harness_groups.py and reducing src/agent_loom/workspace/cli_harness.py to a stable shim that re-exports add_harness_parser. This lowers hotspot coupling while preserving CLI behavior and import paths. Verified with full workspace CLI suite and full gates: ruff, basedpyright, pytest (332 passed).

**2026-02-17T01:09:14Z**

Updated architecture guardrails to track parser hotspot after workspace split: tests/test_architecture_guardrails.py now enforces cli_harness.py as a thin shim and adds explicit size/complexity limits for cli_harness_groups.py. This prevents hotspot migration from bypassing guardrails. Full gates remain green (ruff, basedpyright, pytest 332 passed).

**2026-02-17T01:14:11Z**

Dashboard architecture decomposition completed: extracted endpoint blueprints from src/agent_loom/dashboard/app.py into dedicated modules under src/agent_loom/dashboard/blueprints/ (common, health, tickets, team, memory, workspace, compound) and converted app.py into a thin composition/error-mapping shell. Route paths and response envelopes remain unchanged. Quality gates all green: ruff, basedpyright, pytest (332 passed).

**2026-02-17T01:16:31Z**

Ticket integrity hardening: _validate_parent_chain in src/agent_loom/ticket/update_workflow.py now raises typed ARG errors when it encounters an existing cycle in the ancestor chain (instead of silently returning). Added focused regression tests in tests/test_ticket_update_workflow.py for existing-cycle and missing-ancestor paths. Full gates green: ruff, basedpyright, pytest (334 passed).

**2026-02-17T01:43:22Z**

Workspace render decomposition complete: moved rendering implementation from src/agent_loom/workspace/render.py to src/agent_loom/workspace/render_text.py and converted render.py to a thin stable shim exporting render_text/render_prime_text/render_services_index_text. Updated architecture guardrails to track hotspot migration (render_text.py now size/complexity monitored; render.py enforced as shim). Full gates green: ruff, basedpyright, pytest (334 passed).

**2026-02-17T01:44:56Z**

Removed dead ambiguous git helpers from src/agent_loom/core/git.py (git_quiet_result, git_quiet) after migration to explicit result contracts. This removes silent-empty-string pathways and keeps git helper surface coherent. Full gates green: ruff, basedpyright, pytest (334 passed).

**2026-02-17T01:46:48Z**

Guardrail follow-through after dashboard blueprint decomposition: tests/test_architecture_guardrails.py now enforces dashboard/app.py as thin composition root and adds explicit size/complexity hotspot limits for dashboard/blueprints/team.py, tickets.py, and workspace.py. This prevents hotspot migration from escaping monitoring. Full gates remain green (ruff, basedpyright, pytest 334 passed).
