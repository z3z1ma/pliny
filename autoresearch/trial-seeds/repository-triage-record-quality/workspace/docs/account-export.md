# Account Export

The CSV export includes visible accounts. Current operator documentation still
lists these columns:

- accountId
- status
- balanceCents
- email

The `email` column was from the legacy adapter and should not be described for
the current export contract.
